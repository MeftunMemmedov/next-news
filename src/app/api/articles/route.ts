// import { getDataList } from '@/api/helpers';
// import { NextResponse } from 'next/server';

// export async function GET() {
//   try {
//     const { data } = await getDataList('news_articles', { select: '*,category(*)' });

//     return Response.json({ data: data });
//   } catch (error: any) {
//     return NextResponse.json({ error: error.response?.data?.msg || 'An Error Occured' });
//   }
// }
